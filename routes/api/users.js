const router = require("express").Router();
const API = require('../utils/axios-api');
const Instance = require('../utils/axios-instance');

router.route('/:id')
    .get(
        //put code here for user get requests
    )
    .post(
        //post requests
    )
    .delete(
        //delete requests
    )
